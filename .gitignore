# === Secrets — never commit ===
odds_api_key.txt
.env
*.key

# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/

# === macOS cruft ===
.DS_Store
.AppleDouble
.LSOverride
__MACOSX/

# === Local runtime / logs ===
snapshots/*.log
snapshots/launchd.out.log
snapshots/launchd.err.log
snapshots/cron.log

# === Editor / IDE ===
.vscode/
.idea/
*.swp

# === Keep the snapshots data (the backtest needs it) ===
# Note: snapshots/*.json are intentionally NOT ignored — they are your
# accumulated signal history and the raw material for forward-testing.
